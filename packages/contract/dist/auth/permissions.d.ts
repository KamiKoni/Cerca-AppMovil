import type { Capacity, Permission, PlatformRole } from "./actor";
export declare const CAPACITY_PERMISSIONS: Record<Capacity, readonly Permission[]>;
export declare const PLATFORM_PERMISSIONS: Record<PlatformRole, readonly Permission[]>;
